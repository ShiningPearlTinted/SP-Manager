SP-Manager Deployment Fix

Base: SP-Manager 1.0.17 Cash In / Out Field Fix + Logged-in User Role

Changes in this package:
- Updated GitHub Pages workflow to use Node.js 24.
- Added npm cache using frontend/package-lock.json.
- Kept GitHub Pages permissions and deployment actions unchanged.
- Kept the existing SP-Manager application source unchanged.
- Kept POS Cash In / Out modal, Management Cash In / Out page, permissions,
  Local Agent, and other locked functions unchanged.

Important:
The previous GitHub Actions error was caused by an existing GitHub Pages
 deployment already in progress. This ZIP cannot cancel a server-side
 deployment automatically. If GitHub reports "due to in progress deployment",
cancel the older Pages deployment/run in GitHub Actions, then run the workflow
again.
