# SP-Manager 1.0.45 – POS Category Back Navigation V1.1.25

## Change
- POS group hierarchy is unchanged.
- Back from the top-level `Products > Categories` screen now returns to the POS home menu.
- Categories are no longer immediately rendered again after that Back action.
- Added a small `Categories` entry on the POS home view so the category browser can be reopened.

## Locked
- Local Agent files unchanged.
- Product Group hierarchy logic unchanged.
- Cart, payment, search, printing and other POS functions unchanged.
