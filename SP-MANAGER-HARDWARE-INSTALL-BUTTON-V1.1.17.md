# SP-Manager Hardware Install Button V1.1.17

- Added Install button beside Refresh in Settings > Hardware.
- Local Agent `/status` reports whether `SP-Manager Local Agent Watchdog` is registered.
- Added `/install-autostart` route to register the watchdog for the current Windows user.
- Install button disables after Auto-start is installed.
- Existing hardware/POS functions are unchanged.
