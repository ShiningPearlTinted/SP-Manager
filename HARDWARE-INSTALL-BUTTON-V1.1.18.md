# SP-Manager 1.0.38 – Hardware Install Button V1.1.18

- Fixed the Local Agent BAT installer failure caused by incorrect `schtasks` command escaping.
- Fixed `/install-autostart` so it correctly resolves the Agent and PowerShell paths.
- Auto-start + auto-restart remains a per-user Windows Task Scheduler task.
- Existing POS, printing, cash drawer, customer display, email, products, groups and other functions are unchanged.
